#include <stdio.h>

// Question:  Why is 2+2 != 4?
// %d not associated with variable. edited to fix it.

int main()
{
    int answer; // variable for computation results

    answer = 2+2;

    printf("The answer is %d\n", answer); //variable answer addedi

    return 0;
}
