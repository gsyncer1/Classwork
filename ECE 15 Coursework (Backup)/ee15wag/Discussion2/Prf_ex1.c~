#include <stdio.h> // need this line to use standard i/o functions

/* Demonstrates the use of \n. 
Also shows how to print more than one \n in the same printf() */

int main()
{
   printf("ab");
   printf("cd");
   /* The above strings appear in the same line.  What if we want
    * something to appear on a new line? */

   /* Use \n: the new line "character" */
   printf("\n~\n");
   printf("wxyz\n");
   return 0;
}

